#include "affichage.h"


void affichage(tuile grille[NB_GRILLE][NB_GRILLE]){
    for(int i = 0;i < NB_GRILLE ; i++){
        for(int j = 0; j < NB_GRILLE;j++){
            printf("     %c")
        }
    }
}